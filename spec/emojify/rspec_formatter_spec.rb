require 'spec_helper'

describe Emojify::RspecFormatter do
  it 'has a version number' do
    expect(Emojify::RspecFormatter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
